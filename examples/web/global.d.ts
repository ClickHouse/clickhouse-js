/* eslint-disable no-var */
// `declare var` is the standard way to declare ambient global variables.
declare var CLICKHOUSE_CLOUD_URL: string
declare var CLICKHOUSE_CLOUD_PASSWORD: string
declare var CLICKHOUSE_CLUSTER_URL: string
